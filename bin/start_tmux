#!/bin/bash

function main() {
  local name="$(basename $0)"

  tmux ls | egrep -q "^$name"
  if [ $? -ne 0 ]; then
    TMPDIR=$HOME/.tmux exec tmux new -s $name
  else
    TMPDIR=$HOME/.tmux exec tmux attach -d -t $name
  fi
}

main "$@"
